set you replicate api token as an environmental variable
# PowerShell
$env:REPLICATE_API_TOKEN="your_actual_token_here"

or on Mac
export REPLICATE_API_TOKEN=your_actual_token_here

 echo $env:REPLICATE_API_TOKEN
 to test if it worked
